# Pi Phone Svelte fixtures

These fixture tests exercise the SvelteKit frontend adapters and state reducers without a DOM or backend server.

Run from the repository root:

```sh
npm run frontend:test:fixtures
```

They are also included in root `npm test`.
