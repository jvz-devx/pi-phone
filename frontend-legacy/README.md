# Legacy frontend backup

This folder is a tracked backup of the original static Pi Phone frontend from `public/` before the SvelteKit migration prep on branch `svelte`.

- Runtime assets: `frontend-legacy/public/`
- Original entrypoint: `frontend-legacy/public/index.html`
- Original JavaScript modules: `frontend-legacy/public/app/`
- Original stylesheet: `frontend-legacy/public/styles.css`

Keep this backup until the SvelteKit migration has feature parity and the build output fully replaces `public/`.
